def topKFrequent(self, words, k):
    """
    :type words: List[str]
    :type k: int
    :rtype: List[str]
    """
    count = collections.Counter(words)
    keys = count.keys()
    keys.sort(key = lambda w: (-count[w], w))
    return keys[:k]
